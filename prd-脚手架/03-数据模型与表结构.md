# {项目名称} - 数据模型与表结构

> 版本：v1.0  
> 文档状态：初稿  
> 所属章节：第三章

<!-- ============================================================ -->
<!-- 模板说明：本文档定义所有业务数据的表结构、字段规范、实体关系    -->
<!-- 是后端开发和数据库设计的核心参考                              -->
<!-- 核心章节：功能概述 / 命名规范 / 逐表DDL / 实体关系图         -->
<!-- ============================================================ -->

---

## 一、功能概述

### 1.1 功能定位

本文档定义{项目名称}所有业务数据的**表结构、字段规范、实体关系**，是后端开发和数据库设计的核心参考。涵盖{N}张核心表。

### 1.2 核心概念

| 概念 | 说明 | 涉及表 |
|-----|------|--------|
| {概念1} | {描述} | {表名} |
| {概念2} | {描述} | {表名} |
| {概念3} | {描述} | {表名} |

### 1.3 核心设计原则

1. **{原则1}**：{描述}
2. **{原则2}**：{描述}
3. **{原则3}**：{描述}

---

## 二、业务数据规则

### 2.1 字段命名规范

- 所有表使用 BIGINT 自增主键（id）
- 创建时间统一字段：create_time（DATETIME, DEFAULT NOW）
- 更新时间统一字段：update_time（DATETIME, ON UPDATE）
- 状态字段统一使用 VARCHAR(20) 可读字符串，不使用数字枚举
- 金额字段统一使用 DECIMAL(18,2)
- 外键字段统一使用 BIGINT，命名规则：关联表名_id

### 2.2 {特定数据规则}

- {规则1}
- {规则2}
- {规则3}

---

## 三、核心表结构

### 3.1 表：{表名1}

**说明：** {表用途描述}

**字段列表：**

| 字段名 | 类型 | 长度 | 是否必填 | 主键/索引 | 默认值 | 说明 |
|-------|:----:|:----:|:--------:|:---------:|:-----:|------|
| id | BIGINT | — | Y | PK | AUTO_INCREMENT | 自增主键 |
| {字段名} | VARCHAR | 20 | Y | — | — | {字段说明} |
| {字段名} | DECIMAL | 18,2 | Y | — | 0 | {字段说明} |
| {字段名} | VARCHAR | 20 | N | IDX | — | {字段说明} |
| {字段名} | DATETIME | — | Y | — | NOW | 创建时间 |
| {字段名} | DATETIME | — | Y | — | ON UPDATE | 更新时间 |

**索引策略：**

| 索引名 | 类型 | 字段 | 说明 |
|-------|:----:|------|------|
| idx_{表}_{字段} | INDEX | {字段} | 查询加速 |
| uk_{表}_{字段} | UNIQUE | {字段} | 唯一约束 |
| idx_{表}_{字段1}_{字段2} | INDEX | {字段1},{字段2} | 联合查询 |

**DDL：**

```sql
CREATE TABLE `{表名}` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT COMMENT '自增主键',
  `{字段名}` varchar(20) NOT NULL COMMENT '{字段说明}',
  `{字段名}` decimal(18,2) NOT NULL DEFAULT '0' COMMENT '{字段说明}',
  `create_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`) USING BTREE,
  INDEX `idx_{表}_{字段}` (`{字段名}`) USING BTREE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='{表注释}';
```

### 3.2 表：{表名2}

<!-- 重复 3.1 的模板结构 -->

---

## 四、实体关系图

```mermaid
erDiagram
    {实体A} ||--o{ {实体B} : "一对多"
    {实体A} ||--|| {实体C} : "一对一"
    {实体B} }o--|| {实体D} : "多对一"
    {实体B} ||--o{ {实体B}_item : "明细行"
    {实体E} ||--o{ {实体F} : "一对多"
    
    {实体A} {
        bigint id PK
        varchar field1 "字段说明"
        decimal field2 "字段说明"
        datetime create_time
    }
    {实体B} {
        bigint id PK
        bigint a_id FK
        varchar status "状态"
    }
```

<!-- 实体关系图使用Mermaid的erDiagram语法，清晰展示外键关联关系 -->

---

## 五、并发控制策略

| 表名 | 策略 | 字段 | 说明 |
|-----|------|------|------|
| {表名} | 乐观锁 | version | UPDATE ... SET version=version+1 WHERE version=oldVersion |
| {表名} | 唯一索引 | {字段} | 防止重复插入 |

---

## 六、版本历史

| 版本 | 日期 | 修订内容 |
|:----:|:----:|---------|
| v1.0 | {YYYY-MM-DD} | 初始创建 |
